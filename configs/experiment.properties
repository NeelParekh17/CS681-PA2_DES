# Quick experiment sweep (edit values as needed for final analysis).

sim.cores=4
sim.maxThreads=16
sim.quantum=5ms
sim.ctxSwitch=0.2ms

sim.warmup=1s
sim.measure=3s

dist.think=triangular(20ms, 50ms, 120ms)
dist.service=shifted(2ms, exponential(10ms))
dist.timeout=uniform(30ms, 60ms)

experiment.userCounts=5:50:5
experiment.replications=8
experiment.baseSeed=12345

trace.enabled=false
trace.level=SUMMARY
trace.maxLines=0

