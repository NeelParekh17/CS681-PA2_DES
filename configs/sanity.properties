# Predictable run: no timeouts and constant times.

sim.cores=1
sim.maxThreads=1
sim.quantum=5ms
sim.ctxSwitch=0ms

sim.warmup=0s
sim.measure=1s

dist.think=constant(20ms)
dist.service=constant(5ms)
dist.timeout=constant(1s)

experiment.userCounts=1
experiment.replications=3
experiment.baseSeed=42

trace.enabled=false
trace.level=SUMMARY
trace.maxLines=0

